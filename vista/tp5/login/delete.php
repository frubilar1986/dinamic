<?php

include_once('../../../config.php');
$abmUsuario = new AbmUsuario;
$dataForm = datos_submitidos();
$session = new Session;
 print_r($dataForm);
 print_r($_SESSION);
if (isset($_SESSION['activa']) && $_SESSION['id'] == 1) {
    $
}
